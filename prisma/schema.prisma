model Entry {
  id      Int      @id @default(autoincrement())
  title   String
  summary String   @db.Text
  mood    String
  tags    String   // Comma-separated string
  createdAt DateTime @default(now())

  @@index([tags], name: "idx_entry_tags") // Add this line
}